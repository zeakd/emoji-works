{
  "name": "emoji-works",
  "version": "1.0.0",
  "main": "src/index.html",
  "repository": "https://github.com/zeakd/emoji-works",
  "author": "zeakd <artydeveloperduck@gmail.com>",
  "license": "MIT",
  "scripts": {
    "serve": "node ../parcel/src/cli serve src/index.html --public-url / --out-dir docs/",
    "build": "node ../parcel/src/cli build src/index.html --public-url /emoji-works/ --out-dir docs/ --no-minify"
  },
  "devDependencies": {
    "husky": "^0.14.3"
  },
  "dependencies": {
    "parcel-bundler": "^1.4.1"
  }
}
